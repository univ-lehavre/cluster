# code: language=ansible
- name: Install kubernetes on control plane nodes
  become: true
  become_method: ansible.builtin.sudo
  block:
    - name: Install kubectl
      ansible.builtin.apt:
        name: kubectl
        state: present

    - name: Hold kubectl at its installed version
      ansible.builtin.dpkg_selections:
        name: kubectl
        selection: hold
