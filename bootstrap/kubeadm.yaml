# code: language=ansible
- name: Install Kubernetes
  hosts: cloud
  pre_tasks:
    - name: Audit-log — record playbook execution
      ansible.builtin.import_role:
        name: audit-log
      vars:
        audit_log_playbook: kubeadm.yaml
  roles:
    - k8s-install
