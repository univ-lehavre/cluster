apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rstudio-pvc
  namespace: rstudio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Ti
  storageClassName: rook-ceph-block-replicated
